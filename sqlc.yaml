version: "2"
sql:
  - engine: "sqlite"
    queries: "db/query/"
    schema: "db/migration/"
    gen:
      go:
        package: "sqldb"
        out: "internal/adapter/sqlite/sqldb/"
        emit_json_tags: true
        emit_interface: true
        emit_prepared_queries: false
